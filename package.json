{
  "name": "trainer-engine",
  "private": true,
  "description": "Shared engine for the ullbergm exam-trainer PWAs: spaced-repetition study UI, FSRS scheduler, readiness projection, storage, service worker, structural CSS, and the shared test suites",
  "license": "MIT",
  "scripts": {
    "lint": "eslint",
    "prepare": "git config core.hooksPath .githooks",
    "test": "node tests/fsrs-test.js && node tests/readiness-test.js",
    "test:coverage": "c8 --exclude tests --exclude tools --exclude dist --reporter=text --reporter=text-summary npm test"
  },
  "devDependencies": {
    "@eslint/js": "^10",
    "c8": "^12",
    "eslint": "^10",
    "globals": "^17"
  }
}
